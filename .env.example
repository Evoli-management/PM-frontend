#############################################
# Frontend Environment Example
#############################################
# Copy to `.env` (Vite automatically loads) and adjust.

# Backend API base (must include /api because backend sets global prefix)
VITE_API_BASE_URL=http://localhost:3000/api

# Example feature flags (implement via utils/flags.js reading import.meta.env)
# Prefix with VITE_ to expose to the client.
# VITE_FEATURE_CALENDAR=true
