// ActivityFormModal was the old inline modal used by KeyAreas.
// Re-export the shared CreateActivityFormModal so imports remain valid but the
// implementation is centralized under `src/components/modals/CreateActivityFormModal.jsx`.
export { default } from '../../modals/CreateActivityFormModal';
